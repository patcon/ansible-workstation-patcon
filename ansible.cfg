[defaults]
roles_path = roles_external:roles_custom
inventory = inventory/workstation.ini
# dev-sec.os-hardening 6.2.0 uses `when: sysctl_overwrite | default()` which
# returns a non-boolean; required until the role is updated.
allow_broken_conditionals = true
